<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Group</h1>
  		<%= render 'groups/form', group: @group %>

      <div class="d-flex justify-content-end">
        <%= link_to 'Delete', group_path(@group), method: :delete, class: "btn btn-danger btn-sm", data: { confirm: '本当に消しますか？' } %>
      </div>
  </div>
</div>
